{
    "version": 2,
    "builds": [
        {
            "src": "app.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/templates/(.*)",
            "dest": "/templates/dha.html"
        },
        {
            "src": "/(.*)",
            "dest": "/app.py"
        }
    ]
}
